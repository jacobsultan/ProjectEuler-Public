n = 2
ret = []

ret2 =[]
for i in range(10000):

   
    a = 1 * i
    b = 2 * i
    """c = 3 * i"""
   
    stringa = str(a)
    stringb = str(b)
    """stringc = str(c)"""
    setter = set()
    for let in stringa:
        setter.add(let)
    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)
    """for let in stringc:
        setter.add(let)"""
    if len(setter) == 9 and "0" not in setter:
        ret2.append(i)
print("ret2",ret2)
ret3 = []
for i in range(1,10000):
    
    a = 1 * i
    b = 2 * i
    c = 3 * i
   
    stringa = str(a)
    stringb = str(b)
    stringc = str(c)
    setter = set()
    repeat = False
    for let in stringa:
        setter.add(let)
    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)
    for let in stringc:
        if let in setter:
            repeat = True
            break

        setter.add(let)
    if not repeat:
        if len(setter) == 9 and "0" not in setter:
            ret3.append(i)
print("ret3",ret3)
ret4 = []
for i in range(1000):
    
    a = 1 * i
    b = 2 * i
    c = 3 * i
    d = 4 * i
  

   
    stringa = str(a)
    stringb = str(b)
    stringc = str(c)
    stringd = str(d)
   

    setter = set()
    repeat = False

    for let in stringa:
        setter.add(let)

    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringc:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringd:
        if let in setter:
            repeat = True
            break
        setter.add(let)

 
  
 

    if not repeat:
        if len(setter) == 9 and "0" not in setter:
            ret4.append(i)
print("ret4",ret4)
ret5 = []
for i in range(1000):
    
    a = 1 * i
    b = 2 * i
    c = 3 * i
    d = 4 * i
    e = 5 * i
   
   
    stringa = str(a)
    stringb = str(b)
    stringc = str(c)
    stringd = str(d)
    stringe = str(e)
  

    setter = set()
    repeat = False

    for let in stringa:
        setter.add(let)

    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringc:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringd:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringe:
        if let in setter:
            repeat = True
            break
        setter.add(let)

   

   

    if not repeat:
        if len(setter) == 9 and "0" not in setter:
            ret5.append(i)
print("ret5",ret5)
ret6 = []
ret7 = []
ret8 = []
ret9 = []
for i in range(1000):
    
    a = 1 * i
    b = 2 * i
    c = 3 * i
    d = 4 * i
    e = 5 * i
    f = 6 * i
 

   
    stringa = str(a)
    stringb = str(b)
    stringc = str(c)
    stringd = str(d)
    stringe = str(e)
    stringf = str(f)
   

    setter = set()
    repeat = False

    for let in stringa:
        setter.add(let)

    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringc:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringd:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringe:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringf:
        if let in setter:
            repeat = True
            break
        setter.add(let)

   

  
        

    if not repeat:
        if len(setter) == 9 and "0" not in setter:
            ret6.append(i)
print("ret6",ret6)
for i in range(1000):
    
    a = 1 * i
    b = 2 * i
    c = 3 * i
    d = 4 * i
    e = 5 * i
    f = 6 * i
    g = 7 * i
   

   
    stringa = str(a)
    stringb = str(b)
    stringc = str(c)
    stringd = str(d)
    stringe = str(e)
    stringf = str(f)
    stringg = str(g)
   

    setter = set()
    repeat = False

    for let in stringa:
        setter.add(let)

    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringc:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringd:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringe:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringf:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringg:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    
        

    if not repeat:
        if len(setter) == 9 and "0" not in setter:
            ret7.append(i)
print("ret7",ret7)
for i in range(1000):
    
    a = 1 * i
    b = 2 * i
    c = 3 * i
    d = 4 * i
    e = 5 * i
    f = 6 * i
    g = 7 * i
    h = 8 * i

   
    stringa = str(a)
    stringb = str(b)
    stringc = str(c)
    stringd = str(d)
    stringe = str(e)
    stringf = str(f)
    stringg = str(g)
    stringh = str(h)

    setter = set()
    repeat = False

    for let in stringa:
        setter.add(let)

    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringc:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringd:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringe:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringf:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringg:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringh:
        if let in setter:
            repeat = True
            break
        setter.add(let)

   
        

    if not repeat:
        if len(setter) == 9 and "0" not in setter:
            ret8.append(i)
print("ret8",ret8)
for i in range(1,1000):
    
    a = 1 * i
    b = 2 * i
    c = 3 * i
    d = 4 * i
    e = 5 * i
    f = 6 * i
    g = 7 * i
    h = 8 * i
    k = 9 * k

    print("a",a,"b",b,"c",c,"d",d,"e",e,"f",f,"g",g)

   
    stringa = str(a)
    stringb = str(b)
    stringc = str(c)
    stringd = str(d)
    stringe = str(e)
    stringf = str(f)
    stringg = str(g)
    stringh = str(h)
    stringk = str(k)

    setter = set()
    repeat = False

    for let in stringa:
        setter.add(let)

    for let in stringb:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringc:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringd:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringe:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringf:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringg:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringh:
        if let in setter:
            repeat = True
            break
        setter.add(let)

    for let in stringk:
        if let in setter:
            repeat = True
            break
        setter.add(let)
        

    if not repeat:
        if len(setter) == 9 and "0" not in setter:
            ret9.append(i)
    

print("ret9", ret9)


print(ret)
